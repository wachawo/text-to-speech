## What changed

<!-- The behaviour before and after, not the list of files. -->

## Why

<!-- The motivation, and the issue it closes if there is one: Fixes #NNN -->

## Test plan

<!-- How you verified it: commands run, what you checked by hand. -->

## Checklist

- [ ] `pytest` passes locally
- [ ] `pre-commit run --all-files` passes (black, ruff)
- [ ] Web UI changes: `npm run lint` and `npm test` pass
- [ ] Entry added under `[Unreleased]` in `CHANGELOG.md`
- [ ] Docs updated (`README.md`, `docs/`) where behaviour or config changed
- [ ] English only; no em dash or middle dot characters
