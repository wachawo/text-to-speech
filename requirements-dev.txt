# Development tools - mirrors `[dev]` extras in pyproject.toml. Keep in sync.
# Single-step install: `pip install -e ".[dev]"`.
# Or `pip install -r requirements-dev.txt` for client deps + dev tools without
# editable install (e.g. CI environments that prefer flat requirements files).
-r requirements.txt

# Lint / format / typecheck / test runner
black>=26.5,<27
ruff>=0.14
pre-commit>=4.6.2
mypy>=2.3.1,<3
pytest>=9.1
pytest-cov>=7.1

# Type stubs
# 2.31.0.7+ requires urllib3>=2, but gtts 2.3.0 pins requests~=2.28.0 (urllib3 1.x).
types-requests>=2.33.0.20260906,<2.33.0.20260907

# Web stack - required by tests/ to exercise ttssrv handlers locally and to run
# `python3 ttssrv/app1.py` outside Docker. In production ttssrv runs only inside
# the Docker image; exact pins for it live in ttssrv/requirements.txt.
Flask>=3.1,<4
flask-cors>=6.0,<7
marshmallow>=4.3.1,<5
uvicorn>=0.53,<1
pytz>=2026.3
