# Development tools — mirrors `[dev]` extras in pyproject.toml.
# Single-step install: `pip install -e ".[dev]"`.
# Or `pip install -r requirements-dev.txt` to keep client deps + dev tools.
-r requirements.txt

black==23.3.0
flake8==5.0.4
pytest==7.4.0
pytest-cov==4.1.0

# Optional extras
types-requests==2.28.11.0
types-PyYAML==6.0.12.0
