# Lightweight runtime deps for `ttssrv` - web stack + zero-config engines.
# Used by both Docker images (docker/{cpu,gpu}/Dockerfile pulls this file plus
# its own docker/<flavor>/requirements.txt with the heavy ML stack). Outside
# Docker the same web stack is available via `pip install -e ".[dev]"` for
# tests and local debugging.

# Web stack (exact pins: this is what the Docker images ship)
Flask==3.1.3
flask-cors==6.0.5
marshmallow==4.3.1
uvicorn==0.53.0
pytz>=2026.3

# Lightweight TTS engines (always available on the server)
gtts==2.5.4
pyttsx3==2.99

# Shared infra
python-dotenv>=1.0.0
requests>=2.34.2
sounddevice
numpy
pygame==2.6.1
