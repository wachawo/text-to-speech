# GPU build — PyTorch with CUDA 12.1 wheels (works with NVIDIA driver 525+).
# Installed by ttssrv/Dockerfile.

--extra-index-url https://download.pytorch.org/whl/cu121

-r requirements.txt

torch
torchaudio
coqui-tts[codec]
transformers>=4.46,<5.0
